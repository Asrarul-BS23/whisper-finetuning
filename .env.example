# HuggingFace token — needed for gated datasets (Common Voice) and push_to_hub
HF_TOKEN=
# Where datasets/models are cached (keep off C: if space is tight)
HF_HOME=./data/raw/hf_cache
# Optional MLflow tracking server; unset = local ./mlruns
MLFLOW_TRACKING_URI=
