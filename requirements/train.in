# Training stack — compile ON LINUX (Colab/DGX) so the CUDA wheels resolve.
# DGX Spark (GB10, Blackwell sm_121, aarch64) needs the cu130 index — the plain
# PyPI torch wheel resolves to a CPU-only build on aarch64:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
#   pip-compile --generate-hashes --extra-index-url https://download.pytorch.org/whl/cu130 \
#     --output-file=requirements/train-linux-cu130.txt requirements/train.in
torch==2.6.0    # verify this version exists on the cu130 index before compiling on
                # DGX Spark — bump if not (cu130 aarch64 wheels may start later)
transformers==4.51.0
peft==0.15.0
datasets==3.5.0
accelerate==1.6.0
evaluate==0.4.3
jiwer==3.0.5
audiomentations==0.43.1        # 0.38 caps librosa<0.11 — conflicts with the librosa pin
soundfile>=0.12.1              # let the resolver pick; audiomentations drives this
librosa==0.11.0
ctranslate2==4.5.0
faster-whisper==1.1.0
huggingface_hub>=0.30.0,<1.0
PyYAML==6.0.2
mlflow==2.20.3
tensorboard==2.19.0
dvc[gdrive]==3.59.1
